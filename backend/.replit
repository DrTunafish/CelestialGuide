# Backend configuration for Replit

# Python configuration
[python]
version = "3.12"

# Install dependencies before running
build = "pip install -r requirements.txt"

# Run the FastAPI server
run = "python main.py"

# Watch patterns for auto-reload
[watchers]
any = [
  "*.py",
]

